package com.example.bookworm.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme Grays
val White = Color(0xFFFFFFFF)           // For card backgrounds, prominent surfaces
val LightGray = Color(0xFFF5F5F5)       // For the main screen background
val GraySurface = Color(0xFFE0E0E0)     // For text fields, chips, subtle surfaces
val MediumGray = Color(0xFF757575)      // For secondary text, icons
val DarkGray = Color(0xFF212121)         // For primary text, important elements

// Dark Theme Grays
val AlmostBlack = Color(0xFF121212)     // Standard dark theme background
val DarkSurface = Color(0xFF1E1E1E)     // For cards and surfaces in dark mode
val LightGrayText = Color(0xFFE0E0E0)   // For primary text in dark mode
val MediumGrayDark = Color(0xFF9E9E9E) // For secondary text in dark mode